# Додано колонку порядкового номера у всі таблиці

Summary:

- В усіх представленнях (HR View, Manager View, My View) таблиці тепер мають першу колонку "#" з нумерацією рядків.
- Нумерація оновлюється відповідно до відфільтрованого списку та не залежить від попередніх значень.
- Для Manager View збережені нові колонки без "Підрозділ"; My View отримав аналогічну колонку з рахунком.

Files changed (paths/lines):

- app.js (функції `renderTeamTable`, `renderMyView` — додано колонку "#" та логіку нумерації).
- style.css (новий клас `.col-index` для компактного центрування порядкового номера).

Data/Rules/Functions:

- Не змінювались.

Tests/Validation:

- Manual: перевірити HR View, Manager View, My View — у таблиці перша колонка "#"; застосувати фільтри/перемикання вкладок і переконатись, що нумерація відповідає поточному набору.

Risks & Rollback:

- Низький ризик; потенційно потрібно підлаштувати ширини при додаванні нових колонок. Відкат: видалити логіку в `renderTeamTable`/`renderMyView` та клас `.col-index`.

Next step:

- За потреби додати сортування або залогувати індекс при експорті CSV.

Screens (до/після):

- Не фіксувались.
