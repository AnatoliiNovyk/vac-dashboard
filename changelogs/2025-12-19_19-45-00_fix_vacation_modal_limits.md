# Changelog: 2025-12-19 19:45:00 — Виправлення збереження лімітів у модальному вікні та коригування балансу

## Summary
- Виправлено критичну помилку, через яку зміни ліміту нарахованих днів (поле "НАРАХ.") не зберігалися і блокували закриття модального вікна.
- Перенесено обробку події збереження з `form.submit` на прямий `button.click` для уникнення проблем із кешуванням ES6 модулів.
- Проведено ручне коригування балансу для співробітника Славута О.Д. через виявлений розсинхрон даних у Firestore.

## Files changed
- `index.html`:
    - Рядок 81: змінено `type="submit"` на `type="button"` для кнопки `#vacation-modal-save`.
    - Рядок 343: оновлено версію скрипта `js/main.js?v=20251219b`.
- `js/main.js`:
    - Рядок 62: додано ініціалізацію `vacationLimitInput`.
- `js/modules/vacation-manager.js`:
    - Рядки 27-31: заміна `submit` лісенера на `click` лісенер для кнопки збереження.
    - Рядки 46-54: додано лісенер для інпуту ліміту.
    - Рядки 66, 76: додано завантаження ліміту з профілю співробітника.
    - Рядки 184-203: розширено функцію `saveChanges` для оновлення документа в колекції `employees`.
    - Рядки 270-272: додано відображення ліміту в UI при відкритті.

## Data/Rules/Functions
- **Firestore Data Update**: Для співробітника з ID `2547112539` (Славута О.Д.) оновлено поле `allocation.balanceDays`: було `28`, стало `13` (розрахунок: ліміт 24 - використано 11).
- **Security Rules**: Зміни не потрібні, доступ HRHead дозволяє оновлення `employees` та `vacation_periods`.

## Tests/Validation
1. Тест кнопки "Зберегти": Модальне вікно закривається миттєво після успішного запису (`closeModal(true)`).
2. Тест ліміту: Зміна значення "НАРАХ." у модалці коректно оновлює поле `allocation.totalAllocatedDays` у документі співробітника.
3. Валідація балансу: Після деплою у Славути О.Д. відображається залишок 13 днів.

## Risks & Rollback
- **Ризики**: Прямий `click` лісенер ігнорує стандартну HTML5 валідацію форми (якщо така буде додана).
- **Rollback**: `git checkout js/modules/vacation-manager.js js/main.js index.html`.

## Next step
- Перевірити коректність автоматичного перерахунку `balanceDays` у Cloud Function `updateVacationBalance` при масовому імпорті з BAS.
