# Додано календар у вкладку My View

Summary:

- Персональна вкладка тепер показує той самий календар відпусток, що й HR/Manager View, з актуальними статусами та легендою.
- `renderTeamCalendar` отримав параметр `ignoreFilters`, аби My View показував усі власні відпустки незалежно від глобальних фільтрів.
- `renderMyView` реюзує спільний календар і більше не будує окремий список, забезпечуючи єдиний UX.

Files changed (paths/lines):

- app.js (оновлено `renderTeamCalendar` та `renderMyView`, додано виклик спільного календаря для My View).

Data/Rules/Functions:

- Не змінювались.

Tests/Validation:

- Manual: увійти як співробітник, відкрити My View — перевірити, що календар відображається з подіями/легендою; очистити/змінити відпустки, переконатися що дані синхронізуються.

Risks & Rollback:

- Мінімальний ризик (розшарений календар може залежати від майбутніх змін фільтрів). Відкат: повернути попередню версію `renderMyView` та сигнатуру `renderTeamCalendar`.

Next step:

- За потреби додати персональний список під календарем як вторинний вигляд (опційно).

Screens (до/після):

- Не фіксувались.
