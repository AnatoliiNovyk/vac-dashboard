# Додано кнопку виходу у шапку

Summary:

- Розміщено кнопку `Вийти` у правому верхньому куті шапки, щоб спростити лог-аут незалежно від вкладки чи ролі.
- Додано компактні стилі з hover-ефектом та іконкою для нової кнопки, узгоджені з поточним UI.
- Підключено обробник кліку, що виконує `signOutWithCleanup()` і повертає користувача на екран входу.

Files changed (paths/lines):

- index.html (header-content, додана розмітка кнопки `logout-btn`).
- style.css (нові селектори `.header-logout-btn`, ховер-стилі, налаштування іконки та відступів).
- app.js (`elements.logoutBtn`, `handleLogoutClick`, ініціалізація обробника).

Data/Rules/Functions:

- Зміни у даних або Firebase Rules/Functions відсутні; використовується вже наявний `signOutWithCleanup`.

Tests/Validation:

- Ручна перевірка у браузері з Firebase Emulator Suite: кнопка відображається на всіх вкладках, виконує вихід, повертає на екран входу.

Risks & Rollback:

- Мінімальні ризики (кнопка може перекриватись на надто вузьких екранах). Відкат: видалити нові фрагменти з `index.html`, `style.css`, `app.js`.

Next step:

- 1) Переглянути відображення у темній темі та на мобільних ширинах, коригувати за потреби.

Screens (до/після):

- Не фіксувались.
