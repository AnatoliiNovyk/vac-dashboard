# BAS import UI wiring

## Summary

- added hidden file input and progress indicator within the BAS action block to prepare for import workflows
- connected the HR BAS import button to the new input, invoking the bas-integration stub and updating progress/log messages
- introduced stylesheet rules for the progress track/fill and ensured the module script remains optional-safe

## Files changed

- index.html (added file input and progress markup under the BAS actions)
- style.css (defined styles for the progress container, track, and fill)
- app.js (hooked import button handlers, progress helpers, and integration module access)

## Data/Rules/Functions

- n/a

## Tests/Validation

- not run (DOM wiring only)

## Risks & Rollback

- Low risk: remove the new markup and JS helpers if the BAS import flow needs to be reverted.

## Next step

1. Implement parsing/validation utilities inside `bas-integration.js` and replace the stubbed import/export errors with real logic.

## Screens

- not captured
