# BAS import Firestore sync

## Summary

- extended `bas-integration.js` with invalid-row blocking, Firebase callable invocation, and state sanitisation helpers for committing parsed payloads
- updated HR import workflow to log detailed summaries, abort on high error ratios, and stream commit results/progress to the BAS log UI
- added `importBasData` Cloud Function for chunked upserts of employees/vacations plus integration logging and claim-based access control

## Files changed

- bas-integration.js (invalid ratio guard, commit helper, payload sanitisation)
- app.js (commit wiring, progress/log refinements, callable error handling)
- functions/index.js (BAS import callable with chunked batches and integration logging)

## Data/Rules/Functions

- new callable `importBasData` writes to `employees`, `vacation_periods`, and `integration_logs`
- honours 30% invalid-row stop condition before any backend writes

## Tests/Validation

- not run (needs Firebase Emulator Suite for end-to-end verification)

## Risks & Rollback

- Medium: bulk upserts touch core collections; rollback via removing callable and reverting frontend commit invocation if issues arise.

## Next step

1. Implement export pipeline (date-range filters, Blob download) and add unit/emulator tests for the BAS import/export workflow.

## Screens

- not captured
