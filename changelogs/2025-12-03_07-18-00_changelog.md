# Changelog: 2025-12-03 07:18 — Додано нумерацію рядків у таблиці

## Summary

- Додано колонку "#" (нумерація) першою в таблиці співробітників на всіх вкладках
- Оновлено логіку рендерингу таблиці для відображення порядкового номера

## Changes Made

### HTML (`index.html`)
- Added `<th>#</th>` as the first header in the table template.

### UI Module (`js/modules/ui.js`)
**Requirement**: Додати нумерацію рядків у таблиці.

**Solution**:
- Modified `renderEmployeeTable` to accept `index` in `forEach` loop.
- Added creation of a new `<td>` with `index + 1` as the first cell in each row.
- Updated `colSpan` for the empty state row from 8 to 9 to account for the new column.

## Verification

Протестовано:
- ✅ Колонка "#" відображається першою в заголовку.
- ✅ Кожен рядок має порядковий номер (1, 2, 3...).
- ✅ Працює на всіх вкладках (HR View, Manager View, My View).
- ✅ Manager View коректно відображає нумерацію разом з прихованими колонками.

## Files Modified

### `index.html`
- Line 274: Added `<th>#</th>`

### `js/modules/ui.js`
- Lines 305: Updated `colSpan` to 9
- Lines 313-318: Added logic to render number cell
