# 2024-05-29 12:00:00 – Rebuild app.js

## Summary

- Restored the core client logic after prior file corruption, reimplementing Firebase auth, role-based tab handling, filters, and rendering.
- Reintroduced hierarchical data handling for manager teams and enriched employee datasets for consistent status calculations.

## Technical Details / Diff Highlights

- `app.js`: rebuilt from scratch (~800 lines) with guarded emulator connections, TIN-only login flow, realtime listeners for departments/employees/vacation_periods, and role-aware rendering for HR, Manager, and Employee views.
- `app.js`: updated emulator host detection, fixed Firestore port (8085), and forced login screen/dashboard visibility toggles to prevent overlapping UI after auth.

## Screenshots

- Screenshots not captured yet. Capture HR, Manager, and My View after running locally to confirm UI parity.

## Validation Steps

1. `node scripts/seed.js`
2. `firebase emulators:start`
3. Log in with sample TINs (HR, Manager, Employee) to verify tab visibility, stats, filters, and personal vacation view.

## Rollback Plan

- Restore the previous backup `app.old.js` (if still available) or revert git changes with `git checkout -- app.js`.
