# Changelog: 2025-12-03 07:00 — Відновлено кнопку "Скинути фільтри"

## Summary

- Відновлено кнопку "Скинути фільтри" в HR View та Manager View
- Виправлено помилку доступу до даних департаментів, яка викликала збій інтерфейсу

## Changes Made

### Filters Module (`js/modules/filters.js`)
**Requirement**: Кнопка "Скинути фільтри" зникла після рефакторингу. Потрібно повернути її.

**Solution**:
- Додано логіку створення та додавання кнопки `Reset` у функцію `renderFilters`
- Кнопка з'являється тільки в HR View та Manager View
- Кнопка скидає стан фільтрів (`appState.filters`) та значення селектів

**Bug Fix**:
- Виправлено помилку в циклі рендерингу департаментів: `window.appData.departments.departments.forEach` -> `window.appData.departments.forEach`. Це викликало збій, оскільки `appData.departments` вже є масивом.

## Files Modified

### `js/modules/filters.js`
- Lines 81-89: Added logic to append Reset button
- Lines 149-166: Added `createResetButton` function (if not already present/modified)

## Verification

Протестовано:
- ✅ Кнопка "Скинути фільтри" з'являється в HR View
- ✅ Кнопка "Скинути фільтри" з'являється в Manager View
- ✅ Натискання на кнопку скидає вибрані значення в селектах
- ✅ Інтерфейс не падає при перемиканні вкладок (виправлено помилку з департаментами)

## Result

Функціонал фільтрації повністю відновлено та працює стабільно.
